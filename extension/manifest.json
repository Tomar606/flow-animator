{
  "manifest_version": 3,
  "name": "Flow Animator Bridge",
  "version": "2.0.0",
  "description": "Lets flow-animator drive Google Flow from a background tab. The tab needs no focus and no visible window; the run is driven from the terminal.",
  "permissions": ["alarms", "debugger", "downloads", "scripting", "storage", "tabs"],
  "host_permissions": [
    "https://labs.google/*",
    "https://flow.google/*",
    "http://127.0.0.1:8765/*",
    "http://localhost:8765/*"
  ],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["https://labs.google/*", "https://flow.google/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "action": { "default_title": "Flow Animator Bridge — click to show the status panel" }
}
